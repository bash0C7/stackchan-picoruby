source "https://rubygems.org"

# Local development against a working copy: `bundle config set --local
# local.rb-foundation-model-mac <path to the clone>` (bundler's supported
# local-override mechanism, which requires the git:+branch: form below).
gem "rb-foundation-model-mac", git: "https://github.com/bash0C7/rb-foundation-model-mac.git", branch: "main"
gem "drb"
gem "rinda"
gem "logger"

group :test do
  gem "test-unit"
  gem "rake"
end
